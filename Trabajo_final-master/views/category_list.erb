
<div class="row justify-content-center">
  <div class="col-6">
    <body class='container' >
        <h4 > Categorys - Details </h4 >
        <ul>
          <% @category.each do |category| %>
            <option value="<%= category.category %>"><%= category.category %></option>
          <% end %>
        </ul>
	</body >
  <p><a class="btn btn-primary" href='/'>Home</a></p>
  </div>
</div>

